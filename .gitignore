.DS_Store
coverage/
node_modules/
*.log
# Staging directory used while building a release zip. It is deleted on success, so anything left
# here means packaging failed part way through.
releases/.stage/
